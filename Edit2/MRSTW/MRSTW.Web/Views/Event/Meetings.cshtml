@{
     Layout = "~/Views/Shared/_Layout.cshtml";
     ViewBag.Title = "Meetings";
}

<section>
     <div class="container-fluid meetings">

     </div>
</section>

<!-- @*
<div class="container text-center">
     <h2 class="pb-4">Meetings</h2>
     <div class="row">
          <div class="col-6 col-sm-2 mb-4 mb-lg-0">
               <div class="card">
                    @using (Ajax.BeginForm("EventType", "Meetings", new AjaxOptions { HttpMethod = "POST", OnSuccess = "success", OnFail = "fail" }))
                    {
                         <div class="cat-image">
                              <a class="link_category_events" href="#">
                                   @foreach (var image in Model.Username)
                                   {
                                        @Html.Hidden("btn", image)
                                        <img src=@image class="img-fluid" name="btn">
                                   }
                              </a>
                         </div>
                         <div class="cat-title">
                              @foreach (var events in Model.Events)
                              {
                                   @Html.Hidden("btn", events)
                                   <a href="#" name="btn">@events </a>
                              }
                         </div>
                    }
               </div>
          </div>
     </div>
</div>
*@ -->
